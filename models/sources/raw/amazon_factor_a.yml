version: 2

sources:
  - name: src_amazon_factor_a
    freshness:
      warn_after:
        count: 24
        period: hour
    loaded_at_field: _meta_insert_ts
    schema: raw
    tables:
      - name: amz_fa_dsp_product_report
        description: Raw table with amazon sp-api daily sales report in manufacturing view
