version: 2

sources:
  - name: src_dwh
    schema: dwh
    tables:
      - name: fact_psa_hybris_inventory
        description: traffic light data showing availability of inventory
        tests :
          - dbt_expectations.expect_grouped_row_values_to_have_recent_data:
              group_by: [vg]
              timestamp_column: date_key
              datepart: day
              interval: 1

